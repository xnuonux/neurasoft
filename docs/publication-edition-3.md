# Neurasoft publication, edition 3

Date: 16 September 2026. Owner: Codex, task 019f48a0-45a7-74f0-ad87-cc2c84770946.

## What shipped in the build

The downloaded Neurasoft V2 is the source, not eternities.ai. The original ivory/sage identity, logo, 32 content routes and seven historical journal entries are retained. Four routes are added: an architecture map and three scoped research records. There are 36 navigable content pages plus the custom 404 document.

The home page now leads with the consciousness-research mission and shows concrete research before abstract promises. Authored SVG diagrams replace decorative particle imagery. They describe continuity, revisable interpretation and matched comparisons; they are not fabricated measurements. Motion can be paused, reduced-motion preferences are respected, and mobile architecture uses readable text cards.

Research records NR-2026-001 through NR-2026-003 describe retained-state learning, reopening a belief and learning consequences. Their stronger controls, failed precursor and limits remain prominent. Numerical results are attributed to internal research snapshots, not independently reproduced for this publication. Public JSON summaries are not executable replication packages. No claim of achieved consciousness or automatic transfer into live Luna is made.

Search indexes the full public main content instead of only titles and descriptions. Queries stay in the browser. Hyphen variants, case, multiword matching and exact phrases are handled. The glossary distinguishes action effects from their measurement and belief updating from actual learning. Journal publication dates remain historical, separate from the website edition date. Luna's music is presented as a concrete creative episode, not as a consciousness breakthrough.

## Verification

- 27 inherited site tests, 7 edition tests, 3 public-index tests and 6 JavaScript search tests pass: 43 checks.
- Desktop and 390px mobile browser inspection covers the home, architecture, study detail, menu, search and pause control.
- The educational Observatory run, exact replay and intervention work in the real browser. The page explicitly identifies itself as a local illustration, not a live resident or research engine.
- Cloudflare preview: 49 public files byte-match the build; CSP and nosniff headers pass; unknown routes serve the intended custom 404. Receipt: artifacts/cloudflare-preview-verified.json.
- The legacy portable-preview browser script depends on Linux Chromium and an absent Python Playwright package. It was not executed as a release proof. Hosted-page interactions were verified through the connected browser instead.
- A bounded independent review identified date, evidence-availability and replication-language issues; these were corrected. Its follow-up claim that three old strings remained was contradicted by a fresh read of the final generated pages. Base builder text is overridden by the edition layer; the deployed output is the verification target.

## Deployment and continuity

Cloudflare Pages project: neurasoft-research. Production branch: main. Canonical domain: neurasoft.us. Build locally with `python -X utf8 build.py`, then `python -X utf8 tools/stage_cloudflare.py`; deploy artifacts/cloudflare with Wrangler. Verify a named HTTPS origin using tools/verify_release.py. Do not deploy private/ or the source review archives.

Netlify test deployments are retained as failed hosting attempts, not canonical releases. Their files were accepted but URLs returned HTTP 429. No DNS was pointed there. Do not alter the separate eternities.ai project.

The Superdesign canvas preserves the authored home layout and original logo. Its durable IDs and source fingerprints are in .superdesign/resume.json. Production has no dependency on Superdesign or its uploaded logo URL.

The final domain activation and release receipt will be recorded separately once the registrar and HTTPS checks complete.
